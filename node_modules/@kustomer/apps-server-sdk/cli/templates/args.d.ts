import { ENVS } from '../../lib/constants';
import { App } from '../models';
export interface TemplateFactoryArgs {
    readonly name: string;
    readonly env: keyof (typeof ENVS);
    readonly app: App;
    readonly visibility: string;
    readonly platform?: string;
}
