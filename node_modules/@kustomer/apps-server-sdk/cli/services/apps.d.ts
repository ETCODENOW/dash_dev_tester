import { ENVS } from '../../lib/constants';
import { App } from '../models';
export declare class Apps {
    static findByName(env: keyof typeof ENVS, name: string): Promise<App | undefined>;
    static create(env: keyof typeof ENVS, name: string, url: string): Promise<App>;
}
