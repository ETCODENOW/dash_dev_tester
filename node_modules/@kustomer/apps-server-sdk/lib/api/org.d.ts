import { Org } from '../models';
import { WithID } from '../common';
import { APIBase, APIBaseOptions } from './base';
import { Customers } from './customer';
import { KObjects } from './kobject';
import { Log } from './log';
import { Messages } from './message';
import { Settings } from './settings';
export declare class Orgs<KAppSettings = any> extends APIBase {
    readonly customers: Customers;
    readonly kobjects: KObjects;
    readonly settings: Settings<KAppSettings>;
    readonly messages: Messages;
    readonly log: Log;
    constructor(token: string, orgNameOrId: string, options: APIBaseOptions);
    /**
     * get an org by id
     * @param id org id (unique)
     */
    getById(id: string): Promise<WithID<Org> | undefined>;
}
