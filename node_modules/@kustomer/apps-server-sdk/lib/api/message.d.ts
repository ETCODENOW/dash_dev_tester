import { WithID } from '../common';
import { Message } from '../models';
import { APIBase, APIBaseOptions } from './base';
export declare class Messages extends APIBase {
    constructor(token: string, orgNameOrId: string, options: APIBaseOptions);
    /**
     * get a message by id
     * @param id message id
     */
    getById(id: string): Promise<WithID<Message> | undefined>;
}
