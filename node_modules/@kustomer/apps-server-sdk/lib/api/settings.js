"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Settings = void 0;
const base_1 = require("./base");
class Settings extends base_1.APIBase {
    constructor(token, orgNameOrId, options) {
        super(token, 'settings', orgNameOrId, options);
    }
    /**
     * get your apps settings
     */
    async get() {
        const res = await this.axios.get(`${this.baseUrls.api}/v1/apps/settings`);
        const settings = {};
        Object.keys(res.data.data).map((category) => {
            settings[category] = {};
            for (const setting of res.data.data[category]) {
                settings[category][setting.attributes.name] = setting.attributes.value;
            }
        });
        return settings;
    }
    /**
     * set your apps settings
     * @param settings
     */
    async set(settings) {
        await this.axios.put(`${this.baseUrls.api}/v1/apps/settings`, settings);
    }
}
exports.Settings = Settings;
//# sourceMappingURL=settings.js.map