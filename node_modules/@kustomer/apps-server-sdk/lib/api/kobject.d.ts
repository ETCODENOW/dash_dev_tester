import { WithID } from '../common';
import { KObject, KObjectCreate } from '../models';
import { APIBase, APIBaseOptions } from './base';
export declare class KObjects extends APIBase {
    constructor(token: string, orgNameOrId: string, options: APIBaseOptions);
    /**
     * get a kobject by external id
     * @param externalId external id (unique)
     * @param klass the klass of the kobject
     */
    getByExternalId(externalId: string, klass: string): Promise<WithID<KObject> | undefined>;
    /**
     * update a kobject
     * @param id kobject id (unique)
     * @param klass the klass of the kobject
     */
    update<T, D>(id: string, klass: string, args: KObjectCreate<T, D>): Promise<WithID<KObject>>;
}
